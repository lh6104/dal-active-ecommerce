<?php
/**
 * @var \Magento\Review\Block\Form $block
 */
$product = $block->getProductInfo();
$productImage = (string) $product->getImage();
$imageUrl = ($productImage && $productImage !== 'no_selection')
    ? rtrim($block->getBaseUrl(), '/') . '/media/catalog/product' . $productImage
    : $block->getViewFileUrl('Magento_Catalog::images/product/placeholder/thumbnail.jpg');
$_photoUploadHtml = trim((string) $block->getChildHtml('form_fields_before'));
?>
<div class="block review-add dal-review-modal-root" id="review-form">
    <?php if ($block->getAllowWriteReviewFlag()): ?>
        <div class="dal-review-overlay" data-review-modal-overlay hidden></div>
        <section class="dal-review-modal"
                 role="dialog"
                 aria-modal="true"
                 aria-labelledby="dal-review-modal-title"
                 aria-describedby="dal-review-modal-subtitle"
                 data-review-modal
                 hidden>
            <div class="dal-review-modal__panel" tabindex="-1">
                <button type="button"
                        class="dal-review-modal__close"
                        aria-label="<?= $block->escapeHtmlAttr(__('Close review form')) ?>"
                        data-review-modal-close>
                    <span aria-hidden="true">×</span>
                </button>

                <header class="dal-review-modal__header">
                    <h2 id="dal-review-modal-title"><?= $block->escapeHtml(__('Viết nhận xét')) ?></h2>
                    <p id="dal-review-modal-subtitle"><?= $block->escapeHtml(__('Chia sẻ trải nghiệm của bạn với cộng đồng DAL Active.')) ?></p>
                    <div class="dal-review-product">
                        <img src="<?= $block->escapeUrl($imageUrl) ?>"
                             alt="<?= $block->escapeHtmlAttr($product->getName()) ?>"
                             loading="lazy"/>
                        <strong><?= $block->escapeHtml($product->getName()) ?></strong>
                    </div>
                </header>

                <form action="<?= $block->escapeUrl($block->getAction()) ?>"
                      class="review-form dal-review-form"
                      method="post"
                      enctype="multipart/form-data"
                      id="dal-review-form"
                      data-role="product-review-form"
                      data-bind="scope: 'review-form'">
                    <?= $block->getBlockHtml('formkey') ?>

                    <fieldset class="fieldset review-fieldset dal-review-section" data-hasrequired="<?= $block->escapeHtmlAttr(__('* Required Fields')) ?>">
                        <?php if ($block->getRatings() && $block->getRatings()->getSize()): ?>
                            <span id="input-message-box"></span>
                            <fieldset class="field required review-field-ratings dal-review-rating">
                                <legend class="label">
                                    <span><?= $block->escapeHtml(__('Đánh giá tổng quan')) ?></span>
                                </legend>
                                <div class="control">
                                    <div class="nested" id="product-review-table">
                                        <?php foreach ($block->getRatings() as $_rating): ?>
                                            <div class="field choice review-field-rating">
                                                <span class="dal-review-rating-name" id="<?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_rating_label">
                                                    <?= $block->escapeHtml($_rating->getRatingCode()) ?>
                                                </span>
                                                <div class="control review-control-vote">
                                                    <?php $iterator = 1; foreach ($_rating->getOptions() as $_option): ?>
                                                        <input type="radio"
                                                               name="ratings[<?= $block->escapeHtmlAttr($_rating->getId()) ?>]"
                                                               id="<?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $block->escapeHtmlAttr($_option->getValue()) ?>"
                                                               value="<?= $block->escapeHtmlAttr($_option->getId()) ?>"
                                                               class="radio"
                                                               data-validate="{'rating-required':true}"
                                                               aria-labelledby="<?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_rating_label <?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $block->escapeHtmlAttr($_option->getValue()) ?>_label"/>
                                                        <label class="rating-<?= $block->escapeHtmlAttr($iterator) ?>"
                                                               for="<?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $block->escapeHtmlAttr($_option->getValue()) ?>"
                                                               title="<?= $block->escapeHtmlAttr(__('%1 %2', $iterator, $iterator > 1 ? __('stars') : __('star'))) ?>"
                                                               id="<?= $block->escapeHtmlAttr($_rating->getRatingCode()) ?>_<?= $block->escapeHtmlAttr($_option->getValue()) ?>_label">
                                                            <span><?= $block->escapeHtml(__('%1 %2', $iterator, $iterator > 1 ? __('stars') : __('star'))) ?></span>
                                                        </label>
                                                        <?php $iterator++; ?>
                                                    <?php endforeach; ?>
                                                </div>
                                            </div>
                                        <?php endforeach; ?>
                                    </div>
                                    <p class="dal-review-rating-status" data-review-rating-status>
                                        <?= $block->escapeHtml(__('Chưa chọn sao')) ?>
                                    </p>
                                    <input type="hidden" name="validate_rating" class="validate-rating" value=""/>
                                </div>
                            </fieldset>
                        <?php endif; ?>
                    </fieldset>

                    <fieldset class="fieldset dal-review-section">
                        <div class="field review-field-text required">
                            <label for="review_field" class="label"><span><?= $block->escapeHtml(__('Nhận xét của bạn')) ?></span></label>
                            <div class="control">
                                <textarea name="detail"
                                          id="review_field"
                                          rows="6"
                                          minlength="3"
                                          data-validate="{required:true}"
                                          data-bind="value: review().detail"></textarea>
                                <div class="note"><?= $block->escapeHtml(__('Viết ngắn gọn cảm nhận của bạn về sản phẩm.')) ?></div>
                            </div>
                        </div>
                        <div class="field review-field-summary required">
                            <label for="summary_field" class="label"><span><?= $block->escapeHtml(__('Tiêu đề nhận xét')) ?></span></label>
                            <div class="control">
                                <input type="text"
                                       name="title"
                                       id="summary_field"
                                       class="input-text"
                                       maxlength="150"
                                       data-validate="{required:true}"
                                       data-bind="value: review().title"/>
                            </div>
                        </div>
                        <input type="hidden"
                               name="nickname"
                               id="nickname_field"
                               value="<?= $block->escapeHtmlAttr(__('Khách hàng DAL Active')) ?>"/>
                    </fieldset>

                    <fieldset class="fieldset additional_info">
                        <?= $block->getChildHtml('form_additional_review_info') ?>
                    </fieldset>

                    <div class="actions-toolbar review-form-actions">
                        <div class="primary actions-primary">
                            <button type="submit" class="action submit primary"
                                <?php if ($block->getButtonLockManager()->isDisabled('review_form_submit')): ?>
                                    disabled="disabled"
                                <?php endif; ?>>
                                <span><?= $block->escapeHtml(__('Gửi nhận xét')) ?></span>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </section>
        <script type="text/x-magento-init">
        {
            "[data-role=product-review-form]": {
                "Magento_Ui/js/core/app": <?= /* @noEscape */ $block->getJsLayout() ?>
            },
            "#dal-review-form": {
                "Magento_Review/js/error-placement": {},
                "Magento_Review/js/validate-review": {},
                "Magento_Review/js/submit-review": {}
            }
        }
        </script>
    <?php else: ?>
        <div class="message info notlogged dal-review-login" id="review-form">
            <div>
                <?= $block->escapeHtml(__('Only registered users can write reviews. Please <a href="%1">Sign in</a> or <a href="%2">create an account</a>', $block->getLoginLink(), $block->getRegisterUrl()), ['a']) ?>
            </div>
        </div>
    <?php endif; ?>
</div>
